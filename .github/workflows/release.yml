name: Build & Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    # v0.60/v0.61.1 runs hung 6h in "playwright install --with-deps" (apt).
    # Fail fast instead; green runs take ~7 minutes.
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      # The MCP server is its own package (powernote-mcp/package.json owns
      # `ws`); the root install never touches it, and test:bridge imports it.
      - run: npm ci --prefix powernote-mcp

      - run: npm run build:template

      # A release that cannot be upgraded to is worse than no release, so the
      # regression suite gates the tag. 131 loads the artifact built above.
      - run: npx playwright install --with-deps chromium

      - run: npx playwright test

      - run: npm run test:bridge

      - name: Copy release artifact
        run: cp dist-template/index.html PowerNote.html

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: PowerNote.html
          generate_release_notes: true
          body: |
            ## PowerNote ${{ github.ref_name }}

            **Download `PowerNote.html`** below — open it in any browser to start taking notes.

            No install. No account. No server. Just open and write.

            Your notes are saved inside the file itself — Ctrl+S to download your personalized copy.
